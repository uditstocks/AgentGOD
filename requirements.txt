# The main agent. Generated agents are standard library only and need none of this.
langchain-openai>=1.0,<2.0
langchain-core>=1.0,<2.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0

# The terminal interface. Soft dependency: without it AgentGod
# falls back to plain text output instead of refusing to run.
rich>=13.7,<15

# Development only (tests + lint):
#   pip install pytest ruff
